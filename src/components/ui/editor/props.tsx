import { type EditorProps as TiptapEditorProps } from "@tiptap/pm/view";

export const EditorProps: TiptapEditorProps = {
  attributes: {
    class:
      "prose prose-sm prose-slate font-sans prose-code:before:contents prose-code:after:contents focus:outline-none max-w-full",
  },
};
